class SharkFish extends BiteFish {
  constructor(options) {
    super(options);
    this.imageUri = '/images/MetallicMetallicCheetah-max-1mb.gif'; // set the image to the new image for this fish

    this.maxSurge = 5.0;
    this.width = 103;
  }
}
